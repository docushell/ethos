# ethos-render — Milestone C (WS-OVERLAY). Crops, debug HTML overlay (click-to-highlight). Backs the crop_element API (Milestone D). Joins workspace members when the lane opens.

## Status: Placeholder

This directory is a future-lane marker only. It is not a workspace member, has no Rust source, and
must not be treated as a buildable crate until the Milestone C overlay lane opens.
