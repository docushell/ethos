# ethos-mcp — Milestone D (WS-SURFACES), experimental in Release 1. MCP server wrapping the hardened API 1:1 with PRD §9.4 security rules (allowlisted roots, opaque ids, TTL stores, limits, no traversal). Joins workspace members when the lane opens.
