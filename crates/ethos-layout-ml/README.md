# ethos-layout-ml — Milestone F / Release 2. Optional ONNX/model tier. NEVER part of base; restricted-license/model deps live behind non-default features with license manifests (ADR-0004).

## Status: Placeholder

This directory is a future-lane marker only. It is not a workspace member, has no Rust source, and
must not be treated as a buildable crate until the Milestone F / Release 2 lane opens.
