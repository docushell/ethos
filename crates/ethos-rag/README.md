# ethos-rag — Milestone C (WS-TABLES-RAG). Chunking, citation model, exporters (md/txt/chunks.jsonl). Property: chunker never splits table rows. Joins workspace members when the lane opens.

## Status: Placeholder

This directory is a future-lane marker only. It is not a workspace member, has no Rust source, and
must not be treated as a buildable crate until the Milestone C RAG lane opens.
