# Fixture Harness Results

`make -C benchmarks/harness fixtures` writes the local fixture baseline report here as
`baseline.json`.

These files are run artifacts, not source-of-truth benchmark claims. Published benchmark
numbers must still come from Gate Zero result JSON after the frozen corpus and competitor
pins are complete.
