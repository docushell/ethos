# examples — source-only pre-alpha demo fixture set: RAG demo (parse → chunks → citations → verification), agent demo (verify + crop), foreign-parser demo (verify over OpenDataLoader JSON). Pinned fixtures only.
