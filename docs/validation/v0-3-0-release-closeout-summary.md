# v0.3.0 Release Closeout Summary

The published v0.3.0 release is the current public baseline recorded in
`docs/release-state.json`. Rust crates, the Python wheel, the npm package, the macOS arm64 and
Linux x64 GitHub Release artifacts, public installation wording, package tags, release tag, and
release metadata are closed for that release. Detailed per-lane evidence was retired after
closeout; this compact summary remains solely as the release-state record.
